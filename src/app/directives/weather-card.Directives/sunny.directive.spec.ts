import { SunnyDirective } from './sunny.directive';

describe('SunnyDirective', () => {
  it('should create an instance', () => {
    const directive = new SunnyDirective();
    expect(directive).toBeTruthy();
  });
});
