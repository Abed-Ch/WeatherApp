import { RainyDirective } from './rainy.directive';

describe('RainyDirective', () => {
  it('should create an instance', () => {
    const directive = new RainyDirective();
    expect(directive).toBeTruthy();
  });
});
