import { SnowyDirective } from './snowy.directive';

describe('SnowyDirective', () => {
  it('should create an instance', () => {
    const directive = new SnowyDirective();
    expect(directive).toBeTruthy();
  });
});
